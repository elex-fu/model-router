{
  "name": "model-router",
  "version": "0.1.0",
  "description": "Lightweight AI model proxy with multi-key routing and async logging",
  "main": "dist/cli/index.js",
  "bin": {
    "model-router": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli/index.ts",
    "start": "node dist/cli/index.js",
    "test": "node --test --import tsx --test-reporter=spec 'tests/**/*.test.ts'"
  },
  "keywords": [
    "ai",
    "proxy",
    "anthropic",
    "kimi"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist",
    "scripts",
    "README.md",
    "package.json"
  ],
  "dependencies": {
    "better-sqlite3": "^12.0.0",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
